// import React from 'react';
// import { BranchForm } from '../BranchForm';
// import { shallow } from 'enzyme';
//
// describe('AddBranchForm', () => {
//   let submitSpy;
//   let rendered;

  // beforeEach(() => {
  //   submitSpy = jasmine.createSpy();
  //   rendered = shallow(<AddBranchForm handleSubmit={submitSpy} />);
  // });
  //
  // fit('renders a form header', () => {
  //   const expected = (<header className="details-header">
  //     <span className="title">Add new branch</span>
  //     <span className="actions">
  //       <button className="save" type="submit">Save</button>
  //     </span>
  //   </header>);
  //   console.log(rendered.debug());
  //   expect(rendered.containsMatchingElement(expected)).toEqual(true);
  // });

  // it('renders the form fields', () => {
  //
  // });
  //
  // it('calls handle submit when the form is submited', () => {
  // });
// });

/* <form onSubmit={handleSubmit}>
  <header className="details-header">
    <span className="title">Add new branch</span>
    <span className="actions">
      <button className="save" type="submit">Save</button>
    </span>
  </header>
  <section className="form-container">
    <Field component={InputField} id="name" name="name" label="Name" type="text" />
    <Field component={TextAreaField} id="contact" name="contact" label="Contact" />
    <Field component={TextAreaField} id="notes" name="notes" label="Notes" />
  </section>
</form> */
