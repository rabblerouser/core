import React from 'react';

// img src to be set from CSS.
const PageHeader = () => (
  <header>
    <a href={customisation.signupHomepageLink}><img alt="Logo" /></a>
  </header>
);

export default PageHeader;
