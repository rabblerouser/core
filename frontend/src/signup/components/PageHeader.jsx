import React from 'react';

// img src to be set from CSS.
const PageHeader = () => (
  <header>
    <a href={customisation.signupHomepageLink}><img alt="Sign Up" /></a>
  </header>
);

export default PageHeader;
