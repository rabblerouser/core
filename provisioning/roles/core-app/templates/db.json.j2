
{
    "{{ node_env }}": {
        "username": "{{ db_user }}",
        "password": "{{ db_password }}",
        "database": "{{ db_names[0] }}",
        "host": "{{ db_host }}",
        "port": "{{ db_port }}",
        "dialect": "postgres"
    }
}